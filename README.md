# Simple Login Page (Frontend + Backend)

This is a basic login application built using **HTML, CSS, JavaScript (Frontend)** and **Express.js (Backend)**.  
The project demonstrates how to create a simple login form, send login requests to the backend, and validate credentials

## 🚀 Features
- Simple login form (username & password).
- Backend validation with Express.js.
- Basic API endpoint (`/login`) for authentication.
- Returns "Login Successful" or "Login Failed"

## 🛠️ Tech Stack
- **Frontend:** HTML, CSS, JavaScript  
- **Backend:** Node.js, Express.js  
